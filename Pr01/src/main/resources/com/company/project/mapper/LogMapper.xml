<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
SYSTEM
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.project.mapper.MemberMapper">
	<select id="getMember" resultType="com.company.project.dto.MemberDto">
		select * from member where email = #{email}
	</select>
	
	<select id="selectAllMember" resultType="com.company.project.dto.MemberDto">
		select * from member order by admin asc
	</select>
	
	<select id="confirmEmail" resultType="com.company.project.dto.MemberDto">
		select email from member where email = #{email}
	</select>
	
	<insert id="insertMember">
		insert into member 
		values(#{name}, #{email}, #{pwd}, #{phone}, #{admin})
	</insert>
	
	<update id="updateMember">
		update member set pwd= #{pwd}, 
		phone= #{phone}, 
		admin= #{admin} 
		where email = #{email}
	</update>
	
	<delete id="memberDelete">
		delete from member where email = #{email}
	</delete>
</mapper>